//e1
#include<iostream>
#include<set>
using namespace std;
class Node
{public :
    int nr;
    bool smierc;
    set<int> nastepne;

    Node(int a)
    {
        smierc=false;
        nr=a;
    }
};
class Graf{
public:
 set<Node*> Wierzch;


 void death()
 {
     int *wynik=new int [this->Wierzch.size()];
    set<Node*>::iterator it=this->Wierzch.begin();
    while(*it.smierc==true)
    {
        wynik[*it.nr]=1;
        Wierzch.erase(*it);
    }
    while(*it.nastepne.size()==0)
    {
        
    }

 }


};

int main()
{
    int N_i;
    while(cin>>N_i)
    {Graf G;
        for(int i=0;i<N_i;i++)
        {int a; Node A(i);
            cin>>a;
       for(int j=0;j<a;j++)
        {  int n;
            cin>>n;
            A.nastepne.insert(n);
        }
        if(A.nastepne.size()==0){A.smierc=true;}
             G.Wierzch.insert(&A);
        }
        G.death();

    }
       return 0;
}
